argon2-cffi>=23.1.0
rich>=13.7.0
pyperclip>=1.8.2

# Optional: crypto backend plugins (credmgr/crypto/plugins/). Install
# whichever you plan to use -- credmgr's core never imports these directly.
# Uncomment as needed, or use `pip install credmgr[all]` via pyproject.toml.
# cryptography>=42.0.0    # enables the "aesgcm-cryptography" backend
# PyNaCl>=1.5.0           # enables the "xchacha-pynacl" backend
# pycryptodome>=3.20.0    # enables the "aesgcm-pycryptodome" backend
