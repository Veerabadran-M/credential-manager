argon2-cffi>=23.1.0
cryptography>=42.0.0
rich>=13.7.0
pyperclip>=1.8.2

# Optional: enables the "xchacha20poly1305" cipher choice (config: cipher).
# Not required for the default "aes256gcm" cipher.
pynacl>=1.5.0
